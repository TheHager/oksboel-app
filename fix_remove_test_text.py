# "Remove your fallback 'cloud' icons with 'test' text from the Erhverv page"
# Actually I didn't add 'test' text. The user might have seen it during the HTML View hack because I used standard Container. But wait! I replaced the error widget in 209f12c! No, the fallback in 209f12c is:
# Container(child: const Icon(Icons.storefront, color: Color(0xFFF27C21),))
# Let's verify what's inside Erhverv.
